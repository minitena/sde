pkgname=chroot-fakeroot
pkgver=1.0
pkgrel=1
arch=('any')
source=(fakefakeroot.sh)

build() {
	sed -i fakefakeroot.sh \
		-e "s:@SH_PATH:/usr/bin/sh:" \
		-e "s:@VERSION:${pkgver}:" \
		-e "s:@KEY:fakefakeroot key:"
}

package() {
	install -Dm0755 fakefakeroot.sh $pkgdir/tools/bin/fakeroot
}
